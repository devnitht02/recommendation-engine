# Core
rasa==3.6.21
rasa-sdk==3.6.2

# Rasa dependencies
sanic==21.12.2
Sanic-Cors==2.0.1
sqlalchemy<2.0.0
typing-extensions>=4.2.0

# spaCy (required by SpacyFeaturizer)
spacy==3.5.0
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.5.0/en_core_web_sm-3.5.0-py3-none-any.whl

# Numeric / data
numpy==1.23.5
pydantic<1.10.10

# Django (for your custom KB)
Django==4.2

# Django KB action needs
django-cors-headers==4.7.0

# Optional: if you need to talk to a database
psycopg2-binary==2.9.10      # Postgres
mysqlclient==2.2.7          # MySQL

# HTTP / utils
requests>=2.28.0
PyYAML>=6.0

# (already pulled in by Rasa but good to pin)
werkzeug<3.0.0
itsdangerous<2.2.0
jinja2<3.1.0

# (for training evaluation)
scikit-learn>=1.1.0

# Small helper for safe upgrades
pip-tools>=6.0.0
