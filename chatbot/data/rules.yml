version: "3.1"

rules:
- rule: NLU fallback
  steps:
  - intent: nlu_fallback
  - action: utter_ask_rephrase

- rule: Core fallback
  steps:
  - intent: nlu_fallback
  - action: action_default_fallback
